/**
 * External interface layer providing zmcp tool integration. Handles
 * incoming function calls, parameter validation, and response formatting
 * for file storage operations within the zmcp platform.
 */
package airhacks.zmcp.filestore.boundary;